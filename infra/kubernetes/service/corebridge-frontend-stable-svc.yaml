apiVersion: v1
kind: Service
metadata:
  name: corebridge-frontend-stable
  namespace: corebridge
spec:
  selector:
    app: corebridge-frontend
    version: stable
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
